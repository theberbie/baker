<br />

<div class="booyah-box col-xs-10 col-xs-offset-1">
  <h1> <%=@product.name %></h1>

  <div class="col-sm-4 col-xs-12">
  <span class="label label-danger"> 
    <%= number_to_currency @product.cost %>
    </span>

   <% if current_artisan && current_user == @product.artisan %>
    <br />
    <br />
   <%= link_to "Administer", artisan_product_path(@product), class:'btn btn-danger btn-md' %>
   <% end %>
  </div>


    </div>
    <div class="col-sm-8 col-xs-12">
    <%= @product.description %>
  </div>

</div>